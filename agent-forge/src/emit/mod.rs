//! Emission layer. Renders the audit trail agent-forge already collects into
//! committed markdown documentation beside the code it describes.

/// Filesystem layout and slug generation for emitted documents.
pub mod paths;

/// Typed read model over the forge tables.
pub mod model;
