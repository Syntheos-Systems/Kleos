/// Split text into overlapping chunks of roughly `chunk_size` characters with `overlap` chars of context.
pub fn chunk_text(text: &str, chunk_size: usize, overlap: usize) -> Vec<String> {
    todo!()
}
